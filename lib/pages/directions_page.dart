import 'package:flutter/material.dart';

import '../widgets/widgets.dart';

class DirectionsPage extends StatelessWidget {
  const DirectionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ScanTitles(type: 'http');
  }
}
